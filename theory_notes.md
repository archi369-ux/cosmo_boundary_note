# Theory Notes: Corrected Claims & New Directions

## 1) Correction: ‘global relational information becomes non-reconstructible’
This claim is **rejected** under the RP ontology.
- If T is real and immutable, information is recoverable by rewinding t.
- ‘Non-reconstructible’ only applies in an NR ontology where only the present state + local records exist.

## 2) Dark energy vs space
We explicitly separate:
- Space: the 3D coordinate arena for describing patterns (model S).
- Dark energy: operationally-defined driver of accelerated expansion. citeturn1search12turn1search10

## 3) ‘Big Bang center’ as coordinate convention
We allow choosing an origin O for convenience, but do not assert a physically privileged spatial center in standard cosmology terms. citeturn1search5turn1search10

## 4) Suggested next refinement
Define a minimal notion of a ‘record’ in 3D space (e.g., stable spatial configurations) and specify what it means for ‘no new t updates’ (end) while T remains immutable.
